Pages: about, profile(update/delete post), management

Style: home, post, register, login

Components: post

Dianamic: navbar

Guard: management(admin), create post and profile(user), login and register(guest)

myLinks: state setting

Messages: add interactive massages

!Responsive web

Navbar => if loggedIn profile img with loggedIn link else loggedOut link with icon like bcard

navbar => loggedOut links in mobile